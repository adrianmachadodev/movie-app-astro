---
import { MOVIES } from "../consts/movies"

const moviesNext = MOVIES.filter(movie => movie.year > 1999).splice(0,2)
console.log('movienext', moviesNext);
---
<div
  class="flex flex-row bg-amber-900 w-2/4 min-h-40 justify-center items-center gap-3"
>
  <!-- Card next movie -->
  <div class="flex flex-row w-80 h-40 bg-red-400 rounded-2xl"></div>
</div>
